# Alineacion forzada y transcripcion (las estrategias 2 y 3 del pipeline).
# Requiere ffmpeg en el PATH.
#
# Si quieres usar la GPU, instala PRIMERO requirements-gpu.txt:
#   pip install -r requirements-gpu.txt
#   pip install -r requirements-align.txt
#
# Sin ese paso funciona igual, pero en CPU y mucho mas lento.

torch>=2.1
torchaudio>=2.1
demucs>=4.0
faster-whisper>=1.0
