# Traduccion automatica local (ingles -> espanol u otro idioma).
# El modelo son unos cientos de MB y se descarga la primera vez.
# No necesita clave de API ni conexion despues de esa descarga.
#
# Si quieres usar la GPU, instala PRIMERO requirements-gpu.txt:
#   pip install -r requirements-gpu.txt
#   pip install -r requirements-translate.txt
#
# Sin ese paso funciona igual, en CPU: son unos segundos por cancion.

torch>=2.1
transformers>=4.40
sentencepiece>=0.2
