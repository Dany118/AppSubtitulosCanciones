# Núcleo de lyricsync. Sin GPU, sin modelos: suficiente para buscar letras
# ya sincronizadas, limpiar etiquetas, incrustarlas y usar la interfaz.
#
#   pip install -r requirements.txt
#   pip install -e .

mutagen>=1.47
httpx>=0.27
typer>=0.12
rich>=13.0
numpy>=1.24
